model_params:
  objective: reg:squarederror
  eval_metric: rmse

  random_state: 42
  n_jobs: -1

  n_estimators: 300
  learning_rate: 0.02

  max_depth: 8
  min_child_weight: 10

  gamma: 0.1

  subsample: 0.8
  colsample_bytree: 0.8

  reg_alpha: 0.5
  reg_lambda: 2.0

  early_stopping_rounds: 30